# 决策记录 (DECISIONS.md) - PolicyLens 就业政策智能解读器

> 本文件遵循全局研发宪法，记录 PolicyLens（TRAE AI 创造力大赛参赛作品 · 社会服务与社会公益双赛道）的重大架构选型、商业/社会 ROI、技术壁垒与演进决策。

---

## 决策一：多维规则引擎前置过滤 + 智谱大模型分层防御 (Rule + LLM Hybrid)

### 1. 业务背景与社会痛点
- **政策“找不到、读不懂、办不成”**：全国各地就业补贴政策分散在数百个政府部门网站，条文晦涩漫长，青年与待业群体极难快速判断自己是否符合资格；
- **全量调用大模型的成本不可承受**：若对全库 58 条长篇政策全文直接喂给大模型做端到端匹配，单次查询将消耗数万 Token，不仅延时长达 15~30 秒，且大模型极易出现“幻觉乱配”导致误导申请人。

### 2. 核心架构拍板
1. **0 成本本地规则引擎前置过滤 90%**：
   - 将 58 条非结构化政策抽象解构成 10 类原子准入条件（`AtomicCondition`：身份类型、最高学历、毕业年限、行政地域、就业状态、行业意向、社保记录等）；
   - 用户仅需 3 步填写画像，本地规则引擎毫秒级完成全库扫描、资格打分与补贴金额计算；
   - 编写了 **94 个 Vitest 单元测试**，严密覆盖各种边界与临界情况（如“毕业2年内”窗口期、返乡创业交叉身份等）。
2. **大模型精准触发于“最后一公里翻译”**：
   - 仅当用户需要深入了解匹配成功的 Top 政策时，才按需触发 **智谱 GLM-4.7-Flash** 模型；
   - 严格约束 Prompt 将政务长文翻译为结构化的 10 个直观字段（申报入口、材料清单、经办机构、4 步申请路线图）；
   - 配套后端并发请求去重、缓存与输入校验。

---

## 决策二：端侧性能与求职/分享闭环 (Performance & Social Sharing)

### 1. 架构取舍
- **ECharts 懒加载与代码分割**：ECharts 体积达 ~400KB，若全量打包进首屏将严重拖慢移动端与弱网加载速度。采用 `next/dynamic(() => import('@/components/Dashboard'), { ssr: false })` 实现按需延迟载入；
- **URL 状态无缝自愈与一键分享**：将用户画像紧凑编码为 Base64 Query String（`?p=...`），不仅支持一键生成微信/社媒分享链接，且支持用户刷新或直接收藏 `/report` 时自动对齐默认匹配结果，彻底杜绝白屏与页面崩溃；
- **SSG 预渲染 58 个政策静态页**：利用 Next.js App Router 静态生成所有政策详情页，配合结构化数据 JSON-LD 与 OpenGraph 元标签，兼顾极速加载与搜索引擎收录。
